def getGitCommit() {
    ""
}

def getFrom(){
    if (project.hasProperty('FROM')){
        return FROM
    }else {
        "Source"
    }
}

ext{
    // 获取git commit hashcode
    getGitCommit = this.&getGitCommit
    // 获取来源
    getFrom = this.&getFrom
}
